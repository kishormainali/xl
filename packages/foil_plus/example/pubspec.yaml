name: foil_demo
description: A demonstration of wrapping a widget with Foil.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  xl_plus: 
    path: ../../xl_plus

dev_dependencies:
  foil_plus:
    path: ../

flutter:
  uses-material-design: true
